# CSnake
A Snake Clone using Ncurses

## Installation
**Install dependancies**
```sh
# Arch Linux
pacman -S gcc ncurses
# Fedora
dnf install gcc ncurses-devel
```
**Compile:**
```sh
make
```
**Run:**
```sh
./csnake.out
```

## Controls
- `r` to restart 
- `q` to quit
